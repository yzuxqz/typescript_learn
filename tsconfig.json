{
  // 编译器选项
  "compilerOptions": {
    //指定要用的模块化标准
    "module": "es6",
    //指定ts被编译成的es版本
    "target": "es6",
    //lib用来指定项目中要使用的库
    //"lib": [],
    "sourceMap": true,
    //用来指定编译后文件的目录
    "outDir": "./dist"
    //将代码合并为一个文件,设置后所有的全局作用域的代码会合并到同一个文件中
    //如果需要合并模块的话，规范只能用amd或者system
    //"outFile": "./dist/all.js"

    //是否对js文件进行编译
    "allowJs": false,
    //是否检查js代码是否符合规范
    "checkJs": false,
    //是否移除注释
    "removeComments": true,
    //不生成编译后文件
    "noEmit": false,

    //当有错误时不生成编译后的文件
    "noEmitOnError": true,

    //用来设置编译后的文件是否使用严格模式，默认false，当使用了模块时自动进入严格模式
    "alwaysStrict": false,

    //不允许隐式的any类型
    "noImplicitAny": true,

    //不允许不明确类型的this
    "noImplicitThis": true,

    //严格的检查空值
    "strictNullChecks": true,

    //所有严格检查的总开关
    "strict": true
  },
  //exclude 表示不包含哪些ts文件
  "exclude": [
    "node_modules",
    "./src/hello/**/*"
  ],
  //include 用来指定哪些ts文件需要被编译
  //路径：**表示任意目录
  //      *表示任意文件
  "include": [
    "./src/**/*"
  ]

}
